// The WindowsDesktop SDK's implicit-usings set omits these; declared explicitly so the
// Windows adapters can use System.IO and System.Net.Http without per-file usings.
global using System.IO;
global using System.Net.Http;
