<Window x:Class="LocalDictation.Desktop.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LocalDictation" Width="430" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        FontFamily="{StaticResource FontUi}">

    <Border CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            MouseLeftButtonDown="OnDrag">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#100F12" Offset="0"/>
                <GradientStop Color="#0A0A0B" Offset="0.7"/>
            </LinearGradientBrush>
        </Border.Background>

        <StackPanel Margin="26,24,26,22">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                <Image Source="{StaticResource AppMark}" Width="18" Height="18" VerticalAlignment="Center"/>
                <TextBlock x:Name="TitleText" Style="{StaticResource H2}" Margin="11,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="MessageText" Style="{StaticResource Body}" FontSize="13.5" Margin="0,0,0,22"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource GhostButton}"
                        MinWidth="96" Margin="0,0,10,0" Click="OnCancel"/>
                <Button x:Name="ConfirmButton" Style="{StaticResource PrimaryButton}"
                        MinWidth="120" Click="OnConfirm"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
